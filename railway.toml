[build]
builder = "NIXPACKS"
buildCommand = "echo 'No build command needed - dependencies installed automatically'"

[deploy]
startCommand = "uvicorn api_public:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

